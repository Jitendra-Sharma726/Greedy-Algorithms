bool isValidParenthesisString(string parenthesisString) {
        int low = 0, high = 0;
        for (char c : parenthesisString) {
            if (c == '(') {
                low++;
                high++;
            } else if (c == ')') {
                if (low > 0) low--;
                high--;
            } else {
                if (low > 0) low--;
                high++;
            }
            if (high < 0) return false;
        }
        return low == 0;
    }
